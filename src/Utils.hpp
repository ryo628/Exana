#include <fstream>
#include <string>
#include <sstream>
#include <vector>

#ifndef _UTILS_H_
#define _UTILS_H_

class Utils{
    public:
        static std::vector<std::string> Split(std::string&, char);
};

#endif